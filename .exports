# Make VSCode the default editor
export EDITOR="code -w"

# Setup PATH
# /opt/local/bin/ for `/opt/local/bin/port` only
export PATH="/usr/local/bin:/usr/local/sbin:/opt/local/bin/:$ZSH/bin:$PATH"

# Setup PATH for NVM
export NVM_DIR="$HOME/.nvm"

# Larger bash history (allow 32³ entries; default is 500)
export HISTSIZE=32768
export HISTFILESIZE=$HISTSIZE
export HISTCONTROL=ignoredups
export HISTFILE=~/.zhistory

# Make bash history ignore duplicates and commands such as ls, ll, cd, pwd
export HISTIGNORE="&:l[sla]:cd:cd -:pwd:exit:date:* --help"

# Prefer US English and use UTF-8
export LANG="en_US.UTF-8"
# export LC_ALL="en_US.UTF-8"

# Highlight section titles in manual pages.
export LESS_TERMCAP_md="${yellow}";

# Don’t clear the screen after quitting a manual page
export MANPAGER="less -X"
